import { Link } from 'react-router-dom';

export default () => {

    return (
        <Link to={'/'}>
            <p className={'mb-3 p-0 text-white'}>
                ᐊ BACK
            </p>
        </Link>
    );
}